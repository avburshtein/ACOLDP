# HANDOFF 02 — Полировка результата + Дайджест под чаты LLM

**Для кого:** Cline / другой coding-агент  
**Репо:** ACOLDP  
**Зависит от:** HANDOFF 01 принят (`CASE_DRAFT` = 8 секций UX42) ✅  
**Не делать:** JSON-импорт в UX42, clarify-диалог, AbortController (если не успеваете — вынести), правки Jira API, голос, мультиагенты, большой рефактор REPORT-промпта.

---

## 0. Как запускать в Cline

1. Репозиторий **ACOLDP**.
2. Вставить промпт из §7.
3. Приложить: `@Docs/HANDOFF_02_POLISH_AND_DIGEST.md`
4. Критерий готовности — §6.

---

## 1. Зачем этот пакет

После 01 кейс по структуре правильный, но UI всё ещё выглядит как «только Daily Report», а кнопка «В тикеты» торчит на дайджесте и кейсе (путает дизайнера).  
Дайджест заточен под книги/курсы, а реальный сценарий автора — **дампы чатов с разными LLM**.

Цель: ощущение «инструмент артефактов», не «обёртка над одним отчётом».

---

## 2. Задачи (порядок)

### 2.1 UI результатов — заголовок по режиму

**Файл:** `src/ui/components/results-panel.tsx`

Сейчас при любом Markdown жёстко:

```text
📋 DAILY REPORT
```

Заменить на заголовок от `view.mode`:

| mode | Заголовок в карточке |
|------|----------------------|
| `REPORT` | `📋 Отчёт` |
| `LEARNING_DIGEST` | `📚 Дайджест` |
| `CASE_DRAFT` | `🎨 Кейс UX42` |
| иное | `MODE_LABELS[mode]` |

В шапке панели уже есть `MODE_LABELS[view.mode]` — не дублировать смысл криво; внутри карточки тоже режимный title.

### 2.2 «В тикеты» только для Отчёта

**Файл:** `src/ui/components/results-panel.tsx`

Кнопку «В тикеты» (`onConvert`) показывать **только если** `view.kind === 'report' && view.mode === 'REPORT'`.

Для `LEARNING_DIGEST` и `CASE_DRAFT` оставить: Google Docs / Copy / Download.

### 2.3 Completeness-баннер для кейса (лёгкий UI)

**Файлы:** `src/ui/components/results-panel.tsx` (+ при необходимости крошечный хелпер `src/ui/lib/parse-case-completeness.ts`)

Если `view.mode === 'CASE_DRAFT'` и в markdown есть секция `## Completeness`:

1. Распарсить строку вида: `Заполнено из ввода: 24/42` (числа гибко: `\d+/\d+`).
2. Опционально взять следующую строку «Требует данных дизайнера: …» (обрезать до ~120 символов + «…» если длиннее).
3. Над `<pre>` с markdown показать компактный баннер (без новой дизайн-системы):

```text
Заполнено 24/42 текстовых полей
Ещё нужно: devices, client, … 
```

Если парсинг не удался — баннер не показывать (не ломать UI).  
**Не** вырезать секцию Completeness из самого markdown — она остаётся в тексте для Docs/копирования.

### 2.4 LEARNING_DIGEST — чаты LLM как первоклассный источник

**Файл:** `src/api/prompts.js` → `LEARNING_DIGEST_SYSTEM_INSTRUCTION`

Точечно дополнить (не переписывать весь промпт):

1. **Input sources** явно включать:  
   `LLM chat dump` / смесь чатов (Claude, ChatGPT, Gemini, Qwen, Cline и т.д.), заметки, статьи, курсы.
2. В YAML `source:` допускать значения вроде:  
   `llm-chat` | `llm-chat-mixed` | `book` | `article` | `course` | `video` | `notes` | `mixed` | `unknown`
3. Правило для чатов:  
   - разделять **идеи / решения / открытые вопросы / расхождения между моделями**;  
   - цитаты помечать источником, если в дампе видно (имя модели / роль);  
   - не выдавать домыслы модели за факты пользователя.
4. В `tags` для chat-дампов желательны теги вроде темы + `llm-chat` при уместности.
5. Demo: при необходимости слегка обновить `SAMPLE_LEARNING_DIGEST` в `src/ui/demo-data.ts`, чтобы `source:` был реалистичным (не обязательно chat — но можно один chat-пример, если правки крошечные).

**REPORT не трогать** в этом handoff, кроме случая явного бага, блокирующего сборку.

---

## 3. Файлы — ожидаемый дифф

| Файл | Изменение |
|------|-----------|
| `src/ui/components/results-panel.tsx` | title по mode; «В тикеты» только REPORT; баннер Completeness |
| `src/ui/lib/parse-case-completeness.ts` | опционально: чистый парсер `parseCaseCompleteness(md) → { filled, total, needsDesigner? } \| null` |
| `src/api/prompts.js` | дополнения к LEARNING_DIGEST |
| `src/ui/demo-data.ts` | только если нужно под новый digest / не ломать demo |

---

## 4. Вне скоупа (следующий handoff)

- Abort / Stop запроса  
- JSON export кейса → импорт в `ux42studio`  
- Clarify 1–3 вопроса после кейса  
- Правки тяжёлого `REPORT_SYSTEM_INSTRUCTION`  
- Синхронизация устаревшего `Docs/Project Handover Manifest.md`

---

## 5. Ручная проверка владельцем

1. Режим **Отчёт** → в карточке «Отчёт», кнопка «В тикеты» есть.  
2. Режим **Дайджест** → «Дайджест», «В тикеты» **нет**, экспорт работает.  
3. Режим **Кейс UX42** → «Кейс UX42», «В тикеты» **нет**, над текстом баннер `Заполнено N/M` (на demo sample уже есть Completeness).  
4. Дайджест на куске чата с двумя LLM → в YAML `source` про llm-chat / mixed, в тексте видны расхождения/источники, не вода.

---

## 6. Definition of Done

- [ ] Заголовок результата зависит от `view.mode`  
- [ ] «В тикеты» только при `REPORT`  
- [ ] Completeness-баннер на CASE_DRAFT при успешном парсе  
- [ ] LEARNING_DIGEST явно умеет LLM chat dumps  
- [ ] REPORT / Jira / CASE_DRAFT-промпт (контракт 01) не сломаны  
- [ ] Краткий summary изменённых файлов в ответе агента  

---

## 7. Промпт для агента (копировать в Cline)

```
Задача: полировка UI результатов ACOLDP + доработка LEARNING_DIGEST под дампы чатов LLM.

Прочитай и строго следуй:
@Docs/HANDOFF_02_POLISH_AND_DIGEST.md

Сделай пункты 2.1–2.4. Не трогай CASE_DRAFT-контракт из HANDOFF 01, не делай JSON-импорт в UX42, не переписывай REPORT целиком.

После правок: список файлов + как проверить по §5.
```

---

## 8. После приёмки

Владелец пишет «02 ок» → следующий пакет **HANDOFF 03**: мост кейса в UX42 (JSON / импорт в редактор) **или** Abort+стабильность длинных запросов — выбрать по приоритету.
